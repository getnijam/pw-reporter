{
  "name": "@nijam/pw-reporter",
  "version": "0.1.0-alpha.18",
  "description": "Playwright reporter for Nijam, captures test runs and ships them to the Nijam API.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "playwright",
    "reporter",
    "nijam",
    "test-analytics",
    "flaky-tests"
  ],
  "homepage": "https://nijam.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getnijam/pw-reporter.git"
  },
  "bugs": {
    "url": "https://github.com/getnijam/pw-reporter/issues"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "typecheck": "tsc --noEmit",
    "release": "pnpm build && pnpm publish --access public"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.40.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
